{% extends "base.html" %}

{% block content %}

<table id="categories">
{% for cat in category_tree %}
<tr class="sc-tree-data" data-level="{{ cat.level }}" 
    data-has_children="{{ cat.has_children }}"
    data-label="{{ cat.label }}"
    {% if cat.label_children %}data-label_children="{{ cat.label_children }}"{% endif %}>
<td data-label="rif">{% if cat.is_book %}{{ cat.object.html|safe }}{% else %}{{ cat.object.name }}{% endif %}</td>
</tr>
{% endfor %}
</table>
{% endblock %}

{% block script %}
$("#categories").sc_treeview({title_column: "rif",first_label: "catroot"});

{% endblock %}
