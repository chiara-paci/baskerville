{% extends "base.html" %}

{% block content %}

<table>
<thead>
<tr>
  <th colspan=2>isbn</th>
  <th>publisher</th>
  <th>year</th>
  <th>book</th>
</tr>
</thead>

{% for book in object_list %}
<tbody>
<tr>
  <td class="top">{{ book.isbn_ced }}</td>
  <td class="top">{{ book.isbn_book }}</td>
  <td class="top">{{ book.publisher }}</td>
  <td class="top">{{ book.year }}</td>
  <td class="top">{{ book.html|safe }}</td>
</tr>
</tbody>
{% endfor %}
</table>

{% endblock %}

