{% extends "base.html" %}

{% block content %}

<table>
<thead>
<tr>
  <th colspan=3></th>
  <th>size</th>
  <th>date</th>
  <th colspan=2>format</th>
  <th>description</th>
</tr>
</thead>

{% for photo in photo_list %}
<tbody>
<tr>
  <td class="top">{{ photo.thumb_url }}</td>
  <td class="top">{{ photo.image_url }}</td>
  <td class="top"><a href="{{ photo.get_absolute_url }}">view</a></td>
  <td class="top">{{ photo.width }}x{{ photo.height }}</td>
  <td class="top">{{ photo.datetime }}</td>
  <td class="top">{{ photo.mimetype }}</td>
  <td class="top">{{ photo.format }}</td>
  <td class="top">{{ photo.description }}</td>
</tr>
</tbody>
{% endfor %}
</table>

{% endblock %}

